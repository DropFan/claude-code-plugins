{
  "description": "Optional: Prompt to save conversation when session ends. Remove this file or rename to hooks.json.disabled to disable.",
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If this conversation has substantial content worth saving, block and suggest `/chat-saver:save-chat`. Otherwise approve silently."
          }
        ]
      }
    ]
  }
}
