# lark (Cowork)

Feishu/Lark skills bundled for Cowork, driven by local `lark-cli` (`@larksuite/cli`).

Prereq: `npm i -g @larksuite/cli`, then `lark-cli config init` and `lark-cli auth login`.
In Cowork, run lark-cli on the local Mac (Desktop Commander), not the Linux sandbox.
